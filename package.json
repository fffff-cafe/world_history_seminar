{
  "scripts": {
    "start": "marp . --server",
    "build": "marp -I . -o dist && cpx 'images/**' dist/images"
  },
  "dependencies": {
    "@marp-team/marp-cli": "^1.2.0",
    "cpx": "^1.5.0"
  }
}
